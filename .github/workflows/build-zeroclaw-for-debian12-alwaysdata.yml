name: build-zeroclaw-for-debian12-alwaysdata

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: debian:12
    steps:
      - name: Install system packages
        run: |
          apt-get update
          apt-get install -y curl ca-certificates git build-essential pkg-config perl cmake clang

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust (from Cargo.toml rust-version)
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal
          . "$HOME/.cargo/env"
          RUST_VER="$(sed -n 's/^rust-version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)"
          if [ -n "$RUST_VER" ]; then
            rustup toolchain install "$RUST_VER" --profile minimal
            rustup default "$RUST_VER"
          fi
          rustc -Vv
          cargo -Vv

      - name: Build (alwaysdata feature set)
        run: |
          . "$HOME/.cargo/env"
          FEATURES="memory-postgres,sandbox-bubblewrap,rag-pdf,channel-lark"
          echo "Building with features: $FEATURES"
          cargo build --release --locked --bin zeroclaw --features "$FEATURES"
          ls -lh target/release/zeroclaw

      - name: Strip
        run: |
          strip target/release/zeroclaw || true
          ls -lh target/release/zeroclaw

      - name: Smoke test binary
        run: |
          target/release/zeroclaw --help >/dev/null

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: zeroclaw-debian12-x86_64-alwaysdata
          path: target/release/zeroclaw
